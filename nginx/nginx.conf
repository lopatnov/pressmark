server {
    listen 80;

    # To enable HTTPS, comment out this block and uncomment the one below.
    # For a quick redirect HTTP → HTTPS:
    #   return 301 https://$host$request_uri;

    # Serve React SPA
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    # Proxy gRPC-web (connect-web) to .NET API
    # Trailing slash strips the /grpc prefix before forwarding
    location /grpc/ {
        proxy_pass         http://api:8080/;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
}

# HTTPS server block — uncomment and fill in certificate paths to enable TLS.
# Certificates can be obtained via Let's Encrypt (certbot) or any other CA.
# Mount them into the container by adding a volume in docker-compose.yml:
#   volumes:
#     - /etc/letsencrypt/live/your-domain.com:/etc/nginx/certs:ro
#
# server {
#     listen 443 ssl;
#     server_name your-domain.com;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#     # Serve React SPA
#     location / {
#         root /usr/share/nginx/html;
#         try_files $uri $uri/ /index.html;
#     }
#
#     # Proxy gRPC-web (connect-web) to .NET API
#     location /grpc/ {
#         proxy_pass         http://api:8080/;
#         proxy_http_version 1.1;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto https;
#     }
# }
