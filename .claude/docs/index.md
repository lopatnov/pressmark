# Внутренняя документация для Claude

Это внутренняя память дирижёра (не документация продукта — та в README/CHANGELOG, зона
`technical-writer`). Обновляется дирижёром напрямую по мере необходимости, отдельной роли для
этого нет.

## Карта `.claude/` (для дирижёра)

```
.claude/
├── rules/
│   ├── index.md       — ценности, дисциплина билдов, экономия, ревью PR; точка входа в правила
│   ├── workflow.md    — базовое правило «дирижёр делает сам, кроме...», примеры, DoD
│   ├── team.md         — 9 ролей, зоны ответственности, политика инструментов
│   └── conventions.md — коммиты, версии, ветки, changelog, PR-чеклист
├── agents/
│   ├── _TEMPLATE.md   — канонический шаблон агента (копировать при создании нового)
│   ├── architect.md          — opus; архитектура, декомпозиция, «тяжёлый» рефакторинг
│   ├── server-developer.md   — изолированные backend-подзадачи
│   ├── ui-developer.md       — изолированные frontend-подзадачи
│   ├── tester.md             — тесты всех уровней, включая e2e в браузере
│   ├── security-engineer.md  — чувствительные части, триаж security-алертов
│   ├── devops.md             — CI/CD, Docker, GitHub Actions
│   ├── technical-writer.md   — haiku; README/CHANGELOG по шаблону из diff'а
│   ├── build-validator.md    — haiku; read-only проверка сборки/линта/тестов
│   └── repo-scout.md         — haiku; read-only сбор сигналов для /maintain (deps/security/размер)
├── commands/
│   ├── build.md       — /build: проверка сборки/линта/тестов через build-validator
│   └── maintain.md    — /maintain: плановый проход поддержки (deps, security, refactor+tester,
│                                    build-validator, radar, changelog/technical-writer, PR)
├── skills/
│   ├── _TEMPLATE/               — канонический шаблон скилла (копировать при создании нового)
│   ├── testing/                 — плейбук тестирования (пирамида тестов, роль tester)
│   └── dependency-freshness/    — чек-лист обновления зависимостей до реально последних версий
│                                  (включая мажоры, .NET SDK, React, Docker-образы, GitHub Actions)
└── docs/
    ├── index.md               — этот файл
    └── extending-the-app.md   — как добавить новый gRPC-эндпоинт / новую страницу, по образцу
                                  существующего кода
```

Нет папки `backlog/` — продуктовый бэклог, баги и кандидаты технологического радара (раньше
`backlog/ideas.md`) живут в GitHub Issues проекта (`lopatnov/pressmark/issues`), радар — с меткой
`tech-radar`. Локальные файлы для этого не заводим: одно место правды, видимое пользователю без
захода в репозиторий.

## Порядок чтения при старте сессии
`CLAUDE.md` → `rules/index.md` → `rules/workflow.md` → `rules/team.md`. Дальше — профильные
файлы агентов по мере вызова ролей.

## Почему 9 ролей, а не «лин-ядро» из 13+

Изначальный шаблон принёс 18 ролей, скопированных под общий случай (business-analyst, designer,
lawyer, manager, scrum-master, translator, manual-tester, qa, release-engineer и т.д.). Для
проекта, который уже построен и нуждается в поддержке одной сессией (плюс точечные субагенты),
это оказалось избыточным процессом, не соответствующим реальной нагрузке — большинство ролей ни
разу не вызывались. Роли оставлены только там, где регулярно нужна (а) отдельная экспертиза
(архитектура, безопасность), (б) изоляция шумного вывода (`build-validator`), или (в) достаточно
крупная автономная подзадача. У каждого агента — свой чистый контекст, поэтому дробить роли
дальше есть смысл только когда одной из восьми зон реально не хватает объёмно и регулярно, а не
заранее «на будущее».
